# 🚀 Деплой зараз - 3 команди

## Крок 1: Встановіть залежності (1 хв)

```bash
npm install
```

Це встановить `bcryptjs`, `jsonwebtoken`, `googleapis` локально (потім Vercel зробить те саме на сервері).

---

## Крок 2: Закомітьте і запушіть (1 хв)

```bash
git add .
git commit -m "Fix: bcryptjs dependencies + public API"
git push
```

Vercel автоматично задеплоїть за 1-2 хвилини.

---

## Крок 3: Створіть пароль (2 хв)

1. **Після деплою** відкрийте в браузері (замініть `MyAdmin123` на свій пароль):
   ```
   https://pinguin-v5.vercel.app/api/auth/hash-password?pwd=MyAdmin123
   ```

2. **Скопіюйте `hash`** з відповіді:
   ```json
   {
     "success": true,
     "hash": "$2b$12$abcdef1234567890..."
   }
   ```

3. **Вставте в Google Sheets:**
   - Відкрийте аркуш **Users**
   - Рядок 2, колонка C (password_hash)
   - Замініть `TEMP` на скопійований хеш

4. **Увійдіть:**
   - Логін: `admin`
   - Пароль: `MyAdmin123` (той що ви вигадали)

---

## ❓ Щодо паролів та логів:

### Паролі видно в логах Vercel?
**НІ!** Паролі хешуються на backend. В логах ви побачите тільки хеш (`$2b$12$...`), але не сам пароль.

### Що якщо забуду пароль?
Просто згенеруйте новий хеш через `/api/auth/hash-password` і замініть в Google Sheets.

### Логіни видно в логах?
Так, логіни видно (наприклад `admin`, `nazar`). Але це нормально - логін не секретний, він як email.

### Як зберігати паролі?
Запишіть їх десь безпечно:
- LastPass / 1Password
- Файл на комп'ютері (не в git!)
- Паперовий блокнот

**Приклад:**
```
Пінгвін V5:
Логін: admin
Пароль: MyAdmin123
```

---

## ✅ Готово!

Після цих 3 кроків:
- ✅ index.html працює без авторизації
- ✅ Можете увійти через модальне вікно
- ✅ Дані (заборонені слова, посилання) завантажуються БЕЗ авторизації
- ✅ Entities/Banned Words з'являються після входу

**Успіхів! 🎉**
