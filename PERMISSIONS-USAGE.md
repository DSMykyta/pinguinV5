# Як використовувати систему прав

## Зміст

1. [Основні концепції](#основні-концепції)
2. [Приклади використання](#приклади-використання)
3. [Управління гостями](#управління-гостями)
4. [FAQ](#faq)

---

## Основні концепції

### Що таке "СИСТЕМНА" роль?

- **СИСТЕМНА** - це позначка для вбудованих ролей (`admin`, `editor`, `viewer`)
- Системні ролі **НЕ МОЖНА** видалити через UI
- Це захист від випадкового видалення критично важливих ролей
- Можна редагувати їх права, але не можна видалити

### Структура прав

Права мають формат: `category:resource:action`

**Приклади:**
- `page:users-admin` - доступ до сторінки адмін панелі
- `users:create` - право створювати користувачів
- `panel:aside-table` - доступ до панелі таблиць

### Ролі за замовчуванням

| Роль | Опис | Системна |
|------|------|----------|
| **Admin** | Повний доступ до всіх функцій | ✅ |
| **Editor** | Може читати та редагувати контент | ✅ |
| **Viewer** | Тільки перегляд контенту | ✅ |

---

## Приклади використання

### Приховати кнопку для Editor

```html
<!-- HTML -->
<button id="delete-user-btn">Видалити користувача</button>
```

```javascript
// JavaScript
import { hasPermission, hideIfNoPermission } from './js/utils/permissions.js';

// Варіант 1: Перевірка вручну
if (!hasPermission('users:delete')) {
    document.getElementById('delete-user-btn').style.display = 'none';
}

// Варіант 2: Використання хелпера
hideIfNoPermission('#delete-user-btn', 'users:delete');
```

### Автоматичне приховування через data-атрибут

```html
<!-- HTML -->
<button data-permission="users:create">Додати користувача</button>
<button data-permission="users:edit">Редагувати</button>
<button data-permission="users:delete">Видалити</button>
```

```javascript
// JavaScript
import { initPermissions } from './js/utils/permissions.js';

// Викликати один раз після завантаження сторінки
initPermissions();
```

**Результат:**
- Якщо користувач - `admin`: всі кнопки видимі
- Якщо користувач - `editor`: видно тільки "Додати" та "Редагувати"
- Якщо користувач - `viewer`: всі кнопки приховані

### Вимкнути кнопку замість приховування

```javascript
import { disableIfNoPermission } from './js/utils/permissions.js';

// Кнопка залишається видимою, але неактивною
disableIfNoPermission('#save-btn', 'entities:edit');
```

### Перевірка ролі

```javascript
import { hasRole } from './js/utils/permissions.js';

// Перевірка однієї ролі
if (hasRole('admin')) {
    console.log('Користувач - адміністратор');
}

// Перевірка кількох ролей
if (hasRole(['admin', 'editor'])) {
    console.log('Користувач може редагувати контент');
}
```

### Умовне відображення UI блоків

```javascript
import { hasPermission } from './js/utils/permissions.js';

// Приховати весь блок
const adminPanel = document.getElementById('admin-panel');
if (!hasPermission('page:users-admin')) {
    adminPanel.style.display = 'none';
}

// Динамічне додавання кнопок
const actions = [];

if (hasPermission('entities:edit')) {
    actions.push(`<button class="btn-edit">Редагувати</button>`);
}

if (hasPermission('entities:delete')) {
    actions.push(`<button class="btn-delete">Видалити</button>`);
}

document.getElementById('actions-container').innerHTML = actions.join('');
```

---

## Управління гостями (неавторизованих користувачів)

### Перевірка чи користувач авторизований

```javascript
import { isAuthorized, isGuest } from './js/utils/permissions.js';

if (isGuest()) {
    // Користувач не авторизований
    window.location.href = '/login.html';
}

if (isAuthorized()) {
    // Користувач авторизований
    loadUserData();
}
```

### Показати контент тільки для гостей

```html
<!-- HTML -->
<div id="login-prompt">Увійдіть щоб продовжити</div>
<div id="user-content">Вітаємо, користувач!</div>
```

```javascript
import { showForGuests, showForAuthorized } from './js/utils/permissions.js';

// Показати тільки для гостей
showForGuests('#login-prompt');

// Показати тільки для авторизованих
showForAuthorized('#user-content');
```

### Приклад захисту сторінки

```javascript
// На початку users-admin.html
import { hasPermission } from './js/utils/permissions.js';

if (!hasPermission('page:users-admin')) {
    // Перенаправити на головну або показати помилку
    alert('У вас немає доступу до цієї сторінки');
    window.location.href = '/index.html';
}
```

---

## FAQ

### Як додати нову роль?

1. Перейдіть на таб **"Управління ролями"**
2. Натисніть **"Додати роль"**
3. Заповніть:
   - **Назва ролі** (наприклад: "Content Manager")
   - **ID ролі** (автогенерується: "content-manager")
   - **Опис** (опціонально)
4. Виберіть права з 3 мультиселектів:
   - 🌍 Доступ до Сторінок
   - 🎨 Доступ до Панелей
   - ⚙️ Доступ до Дій
5. Натисніть **"Створити"**

### Як редагувати роль?

1. Відкрийте таб **"Управління ролями"**
2. Натисніть кнопку **редагування** (✏️) біля ролі
3. Змініть дані або права
4. Натисніть **"Зберегти"**

**Примітка:** ID ролі не можна змінювати після створення.

### Чому не можу видалити роль?

Є 2 причини:

1. **Системна роль** - ролі `admin`, `editor`, `viewer` захищені від видалення
2. **Роль використовується** - якщо є користувачі з цією роллю, спочатку треба змінити їх роль

### Як переглянути матрицю доступів?

1. Перейдіть на таб **"Матриця доступів"**
2. Побачите таблицю:
   - **Рядки** - всі можливі права
   - **Колонки** - всі ролі
   - **✅** - роль має право
   - **❌** - роль не має права

### Як створити роль "SEO Specialist"?

**Приклад:**

1. **Назва:** SEO Specialist
2. **ID:** seo-specialist (автогенерується)
3. **Опис:** Може працювати з SEO контентом

**Права:**
- 🌍 **Сторінки:**
  - Головна
  - Глосарій
  - Сутності

- 🎨 **Панелі:**
  - Панель тексту
  - SEO панель
  - Панель перекладу

- ⚙️ **Дії:**
  - Редагування сутності
  - Додавання слова (banned-words)
  - Редагування слова (banned-words)

### Чи можна мати кастомні права?

**Так!** Якщо вам потрібні нові права:

1. Додайте їх у **API** (`api/roles/index.js` → `handleGetPermissionsCatalog`)
2. Додайте в **модалку** (`templates/modals/role-modal.html` → відповідний селект)
3. Додайте в **permissions.js** (`js/utils/permissions.js` → `rolePermissions`)

**Приклад додавання нового права:**

```javascript
// api/roles/index.js
{
  key: 'seo:meta-tags',
  label: 'Управління meta-тегами',
  category: 'actions'
}
```

### Як backend перевіряє права?

**Наразі:** Backend **НЕ** перевіряє детальні права, тільки роль.

**В майбутньому:** Можна додати перевірку на backend:

```javascript
// api/users/index.js
const userPermissions = await getRolePermissions(decoded.role);

if (!userPermissions.includes('users:create')) {
  return res.status(403).json({ error: 'Forbidden' });
}
```

### Де зберігаються дані про ролі?

**Google Sheets:**
- **Roles** - таблиця ролей (id, name, description, is_system)
- **RolePermissions** - таблиця прав (role_id, permission_key, granted)

**Посилання:**
- [Roles таблиця](https://docs.google.com/spreadsheets/d/.../edit#gid=2103009807)
- [RolePermissions таблиця](https://docs.google.com/spreadsheets/d/.../edit#gid=70598913)

---

## Приклад повної інтеграції

```javascript
// entities-manage.js
import {
    hasPermission,
    hideIfNoPermission,
    initPermissions
} from './js/utils/permissions.js';

function renderEntitiesTable(entities) {
    const rows = entities.map(entity => {
        const actions = [];

        // Кнопка редагування (тільки якщо є право)
        if (hasPermission('entities:edit')) {
            actions.push(`<button class="btn-edit" data-id="${entity.id}">Редагувати</button>`);
        }

        // Кнопка видалення (тільки якщо є право)
        if (hasPermission('entities:delete')) {
            actions.push(`<button class="btn-delete" data-id="${entity.id}">Видалити</button>`);
        }

        return `
            <tr>
                <td>${entity.name}</td>
                <td>${entity.description}</td>
                <td>${actions.join(' ')}</td>
            </tr>
        `;
    }).join('');

    document.getElementById('entities-table').innerHTML = rows;

    // Приховати кнопку додавання якщо немає права
    hideIfNoPermission('#add-entity-btn', 'entities:add');

    // Автоматично приховати елементи з data-permission
    initPermissions();
}
```

---

**Корисні посилання:**
- [ROLES-MANUAL.md](./ROLES-MANUAL.md) - повна документація системи ролей
- [permissions.js](./js/utils/permissions.js) - код хелперів
- [api/roles/index.js](./api/roles/index.js) - backend API
