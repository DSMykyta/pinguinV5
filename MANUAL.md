# 📖 Мануал: Адміністрування Користувачів

## 🎯 Навігація

- [Як увійти в систему](#як-увійти-в-систему)
- [Як додати нового користувача](#як-додати-нового-користувача)
- [Як редагувати користувача](#як-редагувати-користувача)
- [Як скинути пароль](#як-скинути-пароль)
- [Як видалити користувача](#як-видалити-користувача)
- [Ролі користувачів](#ролі-користувачів)
- [Технічні деталі](#технічні-деталі)

---

## 🔐 Як увійти в систему

1. Відкрийте будь-яку сторінку сайту (index.html, glossary.html, banned-words.html, тощо)
2. Натисніть кнопку **"Увійти"** внизу лівої панелі
3. Введіть username та password
4. Натисніть **"Увійти"**

> 💡 **Примітка:** Після успішного входу ви побачите своє ім'я та роль внизу лівої панелі.

---

## 👤 Як додати нового користувача

### Крок 1: Відкрийте адмін панель

1. Авторизуйтеся в системі (див. вище)
2. **Важливо:** Ви повинні мати роль **admin**! Інші ролі не мають доступу.
3. В лівій панелі знайдіть пункт **"Користувачі"** (іконка manage_accounts)
4. Натисніть на нього - відкриється сторінка **users-admin.html**

### Крок 2: Створіть користувача

1. Натисніть кнопку **"Додати користувача"** (зверху справа)
2. Заповніть форму:
   - **Ім'я користувача** (username): мінімум 3 символи, латинські літери
   - **Пароль** (password): мінімум 6 символів
   - **Роль** (role): оберіть одну з трьох ролей (див. нижче)
3. Натисніть **"Створити"**

### Приклад:

```
Username: editor_maksym
Password: secure123
Role: Editor
```

> ⚠️ **Важливо:**
> - Username має бути **унікальним**!
> - Пароль автоматично хешується (bcrypt) - ніхто не зможе його побачити
> - Після створення ви не зможете переглянути пароль - тільки скинути новий

---

## ✏️ Як редагувати користувача

1. Відкрийте адмін панель (users-admin.html)
2. Знайдіть потрібного користувача в таблиці
3. Натисніть іконку **"Редагувати"** (олівець)
4. Змініть дані:
   - **Username** - можна змінити
   - **Role** - можна змінити
5. Натисніть **"Зберегти"**

> 💡 **Примітка:** Якщо змінити username на вже існуючий, система покаже помилку.

---

## 🔑 Як скинути пароль

1. Відкрийте адмін панель (users-admin.html)
2. Знайдіть користувача в таблиці
3. Натисніть іконку **"Скинути пароль"** (ключ)
4. Введіть новий пароль (мінімум 6 символів)
5. Підтвердіть пароль (повторіть)
6. Натисніть **"Скинути пароль"**

> ⚠️ **Увага:**
> - Користувач НЕ отримує автоматичне повідомлення про зміну пароля
> - Передайте новий пароль користувачу вручну (через безпечний канал)

---

## 🗑️ Як видалити користувача

1. Відкрийте адмін панель (users-admin.html)
2. Знайдіть користувача в таблиці
3. Натисніть іконку **"Видалити"** (кошик)
4. Підтвердіть видалення
5. Натисніть **"Видалити"**

> 🔒 **Захист:**
> - Ви **НЕ можете** видалити самого себе!
> - Видалення є **незворотнім** - користувач буде повністю видалений з бази

---

## 🎭 Ролі користувачів

### 1. Viewer (Переглядач)
- ✅ Може **читати** дані з Google Sheets
- ❌ **НЕ може** редагувати або додавати
- ❌ **НЕ має** доступу до заборонених слів
- ❌ **НЕ має** доступу до адмін панелі

**Приклад використання:** Контент-менеджер що тільки переглядає глосарій.

### 2. Editor (Редактор)
- ✅ Може **читати** дані з Google Sheets
- ✅ Може **редагувати** та додавати дані
- ❌ **НЕ має** доступу до заборонених слів (тільки admin)
- ❌ **НЕ має** доступу до адмін панелі

**Приклад використання:** Контент-менеджер що працює з глосарієм та сутностями.

### 3. Admin (Адміністратор)
- ✅ Може **читати** дані з Google Sheets
- ✅ Може **редагувати** та додавати дані
- ✅ Має доступ до **заборонених слів**
- ✅ Має доступ до **адмін панелі** (управління користувачами)

**Приклад використання:** Повний контроль над системою.

---

## 🔧 Технічні деталі

### Архітектура API

**Проблема:** Vercel Hobby план має ліміт **12 serverless functions**.

**Рішення:** Об'єднали endpoints з роутингом (12 → 8 → **4 функції**).

#### Поточна структура:

```
📁 api/
├── 📁 auth/
│   └── index.js          ✅ 1 function (login, verify, logout)
├── 📁 sheets/
│   └── index.js          ✅ 1 function (proxy, public, csv)
├── 📁 public/
│   └── data.js           ✅ 1 function
└── 📁 users/
    └── index.js          ✅ 1 function (list, create, update, delete, reset)
```

**Всього: 4 функції з 12 дозволених (67% запасу)**

### Чому 12 запитів - це багато?

**Vercel Hobby (безкоштовний) план:**
- ❌ Максимум 12 serverless functions
- ❌ 100 GB bandwidth/місяць
- ❌ 100 години execution time/місяць

**Якщо перевищити:**
- Сайт перестане працювати (502 Bad Gateway)
- Vercel попросить апгрейд на Pro план ($20/міс)

**Наше рішення:**
- Об'єднали 12 окремих endpoints → **4 unified endpoints**
- Зараз використовуємо **тільки 33%** ліміту
- Можна додати ще **8 endpoints** без проблем!

### Де зберігаються дані?

**Users Database:**
- Google Sheets: `1XE9C6eByiQOoJ_3WNewlMO4QjUpSR-eXI-M6eDn20ls`
- Аркуш: **"Users"**
- Колонки:
  - `A` - id (UUID)
  - `B` - username
  - `C` - password_hash (bcrypt, 12 rounds)
  - `D` - role (admin/editor/viewer)
  - `E` - created_at (ISO timestamp)
  - `F` - last_login (ISO timestamp)

**Доступ:**
- Обмежений доступ (не публічний)
- Service Account: `pinguin-v5-backend@spatial-vision-473814-d7.iam.gserviceaccount.com`

### Безпека паролів

**Хешування:**
- Алгоритм: **bcrypt**
- Salt rounds: **12** (дуже безпечно)
- Приклад хешу: `$2a$12$LQv3c1yqBWVHxkd0LHAkCOYz6TtxMesbjwaD9jaspcHYNik4L/`.

**Що це означає:**
- ✅ Паролі **НЕ зберігаються** в базі
- ✅ Навіть admin **НЕ може** побачити пароль користувача
- ✅ При компрометації бази, хакер **НЕ зможе** отримати паролі
- ✅ Кожен пароль має унікальний salt

---

## 📞 FAQ

### Q: Як дізнатися свою роль?
**A:** Після входу ваша роль відображається внизу лівої панелі під username.

### Q: Що робити якщо забув пароль?
**A:** Зверніться до адміністратора щоб він скинув ваш пароль через адмін панель.

### Q: Чому я не бачу "Користувачі" в меню?
**A:** Цей пункт видимий всім, але доступ до сторінки мають **тільки admin**.

### Q: Скільки користувачів можна створити?
**A:** Технічно необмежено, але рекомендуємо до 100 для оптимальної швидкості.

### Q: Чи можна змінити свій власний пароль?
**A:** Наразі ні - тільки admin може скинути пароль. В майбутньому можна додати цю функцію.

### Q: Що станеться якщо видалити всіх admin?
**A:** ⚠️ **Критична помилка!** Ніхто не зможе керувати користувачами. Завжди залишайте хоча б одного admin!

---

## 🎨 UI Елементи

### Бейджі ролей:

- 🔴 **Admin** - червоний бейдж
- 🟠 **Editor** - помаранчевий бейдж
- 🔵 **Viewer** - синій бейдж

### Форматування дат:

- **Сьогодні, 14:30** - якщо дата сьогодні
- **Вчора, 14:30** - якщо дата вчора
- **12.11.2024** - якщо дата старіша

### Пагінація:

- По замовчуванню: **10** користувачів на сторінці
- Опції: 10, 25, 50, 100, Всі

---

## 🛠️ Для розробників

### Додавання нового endpoint:

Якщо потрібно додати новий endpoint, використовуйте **існуючі unified endpoints**:

```javascript
// ❌ НЕ РОБИТИ:
// api/new-feature/action1.js
// api/new-feature/action2.js
// api/new-feature/action3.js
// = +3 functions

// ✅ РОБИТИ:
// api/new-feature/index.js з роутингом
// = +1 function
```

### Структура unified endpoint:

```javascript
// api/feature/index.js

async function handler(req, res) {
  if (req.method === 'GET') {
    return await handleList(req, res);
  } else if (req.method === 'POST') {
    const { action } = req.body;
    if (action === 'create') return await handleCreate(req, res);
    if (action === 'delete') return await handleDelete(req, res);
  }
}

module.exports = corsMiddleware(handler);
```

---

**Версія:** 1.0
**Дата оновлення:** 14.11.2024
**Автор:** Claude Code

🤖 Generated with [Claude Code](https://claude.com/claude-code)
