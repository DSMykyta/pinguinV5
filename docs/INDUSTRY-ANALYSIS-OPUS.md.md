# Аналітика: Організація JS-коду — Індустрія vs pinguin-v5

> Глибинне дослідження архітектурних патернів організації JavaScript-коду у великих компаніях, open-source фреймворках і build-системах. Порівняння з архітектурою pinguin-v5.

---

## 1. Еволюція підходів до організації JS-коду

Історія модульної JS-архітектури має чітку лінію розвитку від монолітних скриптів до повністю декомпозованих систем.

**2009–2012 — Ера Zakas/Osmani (Yahoo!, Google)**

Фундамент того, що сьогодні називають "scalable JavaScript architecture", заклали Nicholas Zakas (Yahoo!) і Addy Osmani (Google). Їхня тріада — **Application Core → Sandbox/Facade → Modules** — стала каноном:

- **Application Core (Mediator)** — Pub/Sub менеджер, керує життєвим циклом модулів (start/stop/restart). Якщо модуль падає, Core перезапускає його централізовано.
- **Sandbox (Facade)** — обмежений API між Core і модулями. Модуль не бачить Core напряму, лише Sandbox. Додатково — шар permissions: модуль має доступ тільки до дозволених дій.
- **Modules** — ізольовані одиниці функціональності. Не знають одне про одного, спілкуються тільки через Pub/Sub. Можна видалити/додати без впливу на систему.

Ця архітектура використовувалась на Yahoo! Homepage, Google GMail, AOL. Фреймворки Aura і scaleApp — прямі реалізації цієї моделі.

**2013–2018 — Компонентна ера (React, Vue, Angular)**

React змістив фокус з модулів на компоненти. Замість Pub/Sub — однонаправлений потік даних (Flux → Redux → Zustand). Замість Sandbox — Context API. Але базові принципи ізоляції залишились: компонент не знає про сусідів, спілкується через props/events/store.

**2019–2025 — Micro-Frontends і Module Federation**

Масштаб зріс до рівня, де окремі команди деплоять незалежні фронтенд-додатки. Webpack Module Federation, single-spa, qiankun (Alibaba) дозволяють runtime-завантаження віддалених модулів. Netflix, Spotify, IKEA — всі перейшли на мікрофронтенди для команд 15+ розробників.

---

## 2. Порівняльна таблиця архітектур

| Характеристика | Zakas/Osmani (2009) | scaleApp | Aura | Vite/Rollup Plugins | Micro-Frontends | **pinguin-v5** |
|---|---|---|---|---|---|---|
| **Одиниця коду** | Module | Module | Widget | Plugin | Micro-app | Plugin (файл) |
| **Точка входу** | Core.start() | core.start() | app.start() | defineConfig() | Shell App | `-main.js` init() |
| **Комунікація** | Pub/Sub через Mediator | sandbox.on/emit | Pub/Sub через Mediator | Hook lifecycle | Event Bus / Custom Events | `state.registerHook / runHook` |
| **Ізоляція** | Sandbox permissions | Sandbox API | Facade + permissions | Окремий scope | Shadow DOM / iFrame | `state` — єдиний канал |
| **Стан** | Зовнішній store | sandbox.data | Model | Plugin options | Shared store (Pinia/Redux) | `state` object |
| **Відмовостійкість** | Core перезапускає | core.stop/start | Widget lifecycle | Помилка в hook не зупиняє build | Fault isolation | `Promise.allSettled` + `try/catch` |
| **Видалення модуля** | core.stop(id) | core.stop(id) | app.stop(widget) | Видалити з plugins[] | Відключити remote | Видалити файл |
| **DOM** | Через Sandbox | dom plugin | Через Facade | Не стосується | Web Components | Кешування в `state.dom`, логіка в `-core.js` |
| **Template** | Вільний | template plugin | View | Не стосується | Своя технологія | `-template.js` (чистий HTML-рядок) |

---

## 3. Детальне порівняння з ключовими системами

### 3.1 Zakas/Osmani Scalable Architecture

**Що спільного з pinguin-v5:**
- Модулі не знають одне про одного — ідентичний принцип (Закон II)
- Комунікація тільки через центральну шину (Pub/Sub ≈ Hooks)
- Модуль можна видалити без впливу на систему (Закон III)
- Application Core ≈ `-main.js` + `-core.js`
- Sandbox ≈ `state` object

**Де pinguin-v5 простіший:**
- Zakas має 3 шари (Core → Sandbox → Module), pinguin-v5 — 2 шари (state → plugin)
- Zakas має permissions layer (модуль не може emit на заборонений канал). В pinguin-v5 нема обмежень — будь-який плагін підписується на будь-який хук
- Zakas має lifecycle (start/stop/restart). В pinguin-v5 — тільки init. Немає механізму зупинки/перезапуску конкретного плагіна

**Де pinguin-v5 суворіший:**
- Zakas не має поділу Core на 4 файли. В pinguin-v5 — жорсткий контракт: main/state/template/core
- Zakas не формалізує де живе DOM-логіка. В pinguin-v5 — чітке розмежування (template не вставляє, state не маніпулює)
- pinguin-v5 вимагає `Promise.allSettled` — без виключень. У Zakas це рекомендація

### 3.2 Vite/Rollup Plugin System

Vite побудований на Rollup-сумісній плагінній архітектурі з lifecycle hooks:

```js
// Vite plugin — об'єкт з hooks
export default function myPlugin() {
    return {
        name: 'my-plugin',
        buildStart() { /* ... */ },
        transform(code, id) { /* ... */ },
        buildEnd() { /* ... */ },
    }
}
```

**Що спільного з pinguin-v5:**
- Plugin — об'єкт/функція з чітким контрактом
- Комунікація через hooks, не через прямі виклики
- Plugin не знає про інші plugins
- Можна додати/видалити без впливу на систему

**Ключові відмінності:**
- Vite hooks мають чітку послідовність (buildStart → resolveId → load → transform → buildEnd). В pinguin-v5 хуки — паралельні, порядок не гарантований
- Vite plugins можуть повертати значення (transform повертає модифікований код). В pinguin-v5 hooks — fire-and-forget, без return
- Vite має `enforce: 'pre' | 'post'` для контролю порядку. В pinguin-v5 порядок виконання хуків не контролюється
- Vite plugins мають доступ до повного config через `configResolved`. В pinguin-v5 плагін бачить тільки `state`

### 3.3 Webpack Plugin System

Webpack має найскладнішу plugin-систему — Tapable hooks з різними типами:

- **SyncHook** — синхронний, без повернення
- **SyncBailHook** — зупиняється якщо callback повертає значення
- **SyncWaterfallHook** — передає результат наступному callback
- **AsyncSeriesHook** — послідовний async
- **AsyncParallelHook** — паралельний async

**Порівняння з pinguin-v5:**
pinguin-v5 має лише один тип хука — аналог AsyncParallelHook (всі callbacks викликаються паралельно, без передачі результатів). Це свідомий trade-off: простота замість гнучкості. Для адмін-панелі e-commerce — достатньо. Для build-системи — ні.

### 3.4 Micro-Frontends (Netflix, Spotify, Alibaba)

**Масштаб проблеми:**
Мікрофронтенди вирішують організаційну проблему: 15+ розробників, 3+ команд, незалежний deployment. Технології: Module Federation (Webpack 5), single-spa, Web Components.

**Порівняння:**
- pinguin-v5 — це архітектура одного проєкту однієї команди. Мікрофронтенди — між-командна архітектура
- Мікрофронтенди оперують на рівні додатків (Shell → Remote Apps). pinguin-v5 — на рівні файлів (main → plugins)
- Мікрофронтенди мають runtime federation (завантаження з CDN). pinguin-v5 — dynamic import локальних файлів
- Спільне: ізоляція, незалежний deployment (видалення файлу ≈ відключення remote), fault isolation

### 3.5 Web Components (браузерний стандарт)

**Custom Elements + Shadow DOM + HTML Templates** — нативний браузерний підхід до компонентизації.

**Порівняння:**
- Web Components дають encapsulation через Shadow DOM (ізоляція стилів). pinguin-v5 ізолює через архітектурні правила (конвенції), не через технічні обмеження
- Web Components — framework-agnostic. pinguin-v5 — vanilla JS за замовчуванням
- Шарми pinguin-v5 (HTML-атрибут → поведінка) концептуально близькі до Custom Elements: `<div pagination="25">` vs `<my-pagination size="25">`

### 3.6 scaleApp Framework

Найближчий аналог pinguin-v5 серед існуючих фреймворків.

```js
// scaleApp module
core.register("myModule", function(sandbox) {
    return {
        init: function() { sandbox.on("event", handler); },
        destroy: function() { /* cleanup */ }
    };
});
```

**Спільне:**
- Module отримує sandbox (≈ state) як єдиний параметр
- init() — єдина точка входу
- Комунікація через on/emit (≈ registerHook/runHook)
- Core керує lifecycle

**Відмінності:**
- scaleApp має destroy() для cleanup. pinguin-v5 — тільки init()
- scaleApp має permission plugin. pinguin-v5 — без permissions
- scaleApp має sub-modules (вкладені модулі). pinguin-v5 — плоска структура
- scaleApp — runtime фреймворк з API. pinguin-v5 — архітектурна конвенція без залежностей

---

## 4. Патерни комунікації: порівняльний аналіз

| Патерн | Як працює | Хто використовує | pinguin-v5 |
|---|---|---|---|
| **Pub/Sub** | Підписка на іменований канал, emit з payload | Zakas, scaleApp, Aura, EventEmitter | ✅ Через registerHook/runHook |
| **Mediator** | Центральний об'єкт координує всіх | Gmail, Yahoo!, chat systems | ⚠️ Частково — state виконує роль mediator |
| **Observer** | Subject → notify observers | DOM events, RxJS | ❌ Не використовується напряму |
| **Command/Query** | Запит повертає дані, команда змінює стан | CQRS, Redux dispatch | ❌ Хуки — fire-and-forget |
| **Waterfall** | Результат одного callback → input наступного | Webpack Tapable, Express middleware | ❌ Хуки паралельні |
| **Event Sourcing** | Кожна зміна — іммутабельна подія | CQRS/ES backends, Redux DevTools | ❌ State мутабельний |

**Висновок:** pinguin-v5 реалізує Pub/Sub в чистому вигляді — без waterfall, без return values, без middleware chain. Це найпростіший з можливих підходів, але достатній для DOM-орієнтованих плагінів.

---

## 5. Аналіз контракту рівнів pinguin-v5

Контракт "один файл — одна роль" (main/state/template/core/plugin) — це унікальна формалізація, якої немає в жодній з досліджених систем.

### Де це зустрічається (частково):

- **MVC/MVVM** — Model/View/Controller — розділення ролей, але без формалізації файлів
- **Angular** — .component.ts / .service.ts / .module.ts — суворий поділ, але інші ролі
- **Remix/Next.js** — loader/action/component — чіткий контракт, але на рівні роутів
- **Feature-Sliced Design (FSD)** — entities/features/shared/widgets — шари, але на рівні папок

### Що pinguin-v5 робить інакше:

pinguin-v5 формалізує на рівні **суфікса файлу** (`-main.js`, `-state.js`, `-template.js`, `-core.js`). Це більш гранулярний підхід ніж папки (FSD) чи типи файлів (Angular). Суфікс одразу говорить що дозволено, а що заборонено — навіть без відкриття файлу.

---

## 6. Шарми — порівняння з аналогами

Шарми (HTML-атрибут → JS-поведінка) мають прямі аналоги:

| Система | Підхід | Приклад |
|---|---|---|
| **Alpine.js** | `x-` директиви в HTML | `<div x-show="open">` |
| **htmx** | `hx-` атрибути | `<button hx-get="/api">` |
| **Stimulus (Hotwire)** | `data-controller` / `data-action` | `<div data-controller="search">` |
| **Web Components** | Custom Elements | `<my-pagination size="25">` |
| **jQuery plugins** | `data-` атрибути + $.fn | `<div data-toggle="modal">` |
| **pinguin-v5 Шарми** | Custom/data- атрибути + делегування | `<div pagination="25">` |

**Найближчий аналог — Stimulus (Basecamp/37signals):**
- Stimulus: контролери — окремі JS-файли, активуються через `data-controller`
- Шарми: charm-функції в окремих файлах, активуються через HTML-атрибути
- Обидва: делегування на document, zero JS на рівні сторінки, declarative HTML

**Відмінність:** Stimulus має повноцінний lifecycle (connect/disconnect/value change callbacks). Шарми — одноразова ініціалізація через делегування. Stimulus — framework. Шарми — конвенція.

---

## 7. Відмовостійкість: порівняння підходів

| Система | Що відбувається при падінні модуля |
|---|---|
| **Zakas/Osmani** | Core може перезапустити модуль. Error Handling централізований |
| **scaleApp** | core.stop() → core.start(). Lifecycle callbacks для cleanup |
| **Micro-Frontends** | Fault isolation — один micro-app падає, інші працюють |
| **Vite plugins** | Build не падає від одного плагіна (залежить від hook type) |
| **React Error Boundaries** | componentDidCatch ловить помилки в дереві компонентів |
| **pinguin-v5** | `Promise.allSettled` для завантаження + `try/catch` в кожному хуці |

pinguin-v5 реалізує відмовостійкість на двох рівнях: завантаження (allSettled) і виконання (try/catch в runHook). Це мінімально достатній підхід. Більш зрілі системи додають: retry logic, fallback UI, graceful degradation, health checks.

---

## 8. Сильні сторони pinguin-v5

1. **Нуль залежностей** — чистий vanilla JS. Не потрібен ні framework, ні build tool для роботи архітектури. Zakas потребував AMD/RequireJS, scaleApp — окремий пакет, мікрофронтенди — Webpack/Vite.

2. **Екстремальна формалізація** — контракт рівнів з таблицею "зобов'язаний / заборонено" — рідкість. Більшість систем описують що модуль *може* робити. pinguin-v5 описує що *заборонено*. Це ефективніше для підтримки дисципліни.

3. **Таблиця порушень** — окремий розділ "якщо бачиш це — рефактор обов'язковий" — прагматичний підхід, якого немає в жодній документації з досліджених.

4. **Шарми** — елегантне рішення для повторюваних UI-поведінок. Найближчий аналог — Stimulus, але без додаткового framework.

5. **Візуальні шапки файлів** — Unicode-рамки з іконками типу (`🔒 ЯДРО`, `🔌 ПЛАГІН`) — instant visual classification. Жодна з досліджених систем не формалізує header файлів на такому рівні.

---

## 9. Потенційні точки росту

1. **Lifecycle management** — в поточній архітектурі немає destroy(). Zakas, scaleApp, Stimulus мають повний lifecycle (init → connected → disconnected → destroy). Для SPA з динамічним контентом cleanup стає критичним (memory leaks, event listener накопичення).

2. **Typed hooks** — хуки в pinguin-v5 — всі однотипні (fire-and-forget). Webpack Tapable показує цінність різних типів: SyncBailHook (зупинити ланцюжок), SyncWaterfallHook (передати результат далі), AsyncSeriesHook (послідовний async).

3. **Hook priorities** — порядок виконання хуків не контролюється. Event systems в WordPress, scaleApp мають пріоритети. Це стає важливим коли один плагін повинен виконатись до іншого.

4. **State immutability** — state мутабельний, будь-хто може записати будь-що. Proxy-based reactivity (Vue 3 підхід) або immer-style immutable updates могли б додати передбачуваність і debug-здатність.

5. **DevTools / Debug mode** — великі plugin-системи мають debug-інструменти: Vite виводить plugin stats, Redux має DevTools, scaleApp має logging plugin. Для pinguin-v5 лог всіх hook-викликів і plugin-завантажень був би корисним.

---

## 10. Загальний висновок

pinguin-v5 реалізує перевірену архітектуру (модулі + event bus + ізоляція), яка була формалізована ще у 2009 році Zakas/Osmani і з тих пір не втратила актуальності. Ця модель лежить в основі Gmail, Yahoo!, сотень enterprise-додатків, і залишається валідною в 2025.

Головна цінність pinguin-v5 — не в унікальності патернів (вони класичні), а в **рівні формалізації**: жорсткий контракт рівнів, таблиця порушень, суфіксна типізація файлів, візуальні шапки. Це перетворює архітектурні рекомендації (які зазвичай ігнорують) на архітектурні правила (які видно одразу).

В порівнянні з індустрією, pinguin-v5 стоїть між:
- **Zakas/Osmani** (менше шарів, простіший lifecycle)
- **Stimulus** (схожий declarative підхід для шармів)
- **scaleApp** (найближча паралель за контрактом модулів)
- Менш складний ніж Webpack/Vite plugins (один тип хуку замість багатьох)
- Менш масштабний ніж Micro-Frontends (один проєкт, одна команда)

Архітектура адекватна для свого контексту: адмін-панель e-commerce з десятками модулів, де одна людина підтримує кодову базу і потребує максимальної передбачуваності.

---

*Джерела: Addy Osmani — Patterns For Large-Scale JavaScript Application Architecture; Nicholas Zakas — Scalable JavaScript Application Architecture (Yahoo!); scaleApp framework; Vite Plugin API; Webpack Tapable; CSS-Tricks — Designing a JavaScript Plugin System; freeCodeCamp — Event-Based Architectures in JavaScript; Martin Fowler — Micro Frontends; patterns.dev*
