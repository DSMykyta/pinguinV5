# ЗВІТ ПРО ПОРУШЕННЯ АРХІТЕКТУРНИХ ПРИНЦИПІВ

> **Дата аналізу:** 2025-01-15
> **Версія проєкту:** v4 ALL ALL
> **Статус:** 10 порушень знайдено (1 критичне, 4 середніх, 5 низьких)

---

## 📊 ЗАГАЛЬНА СТАТИСТИКА

| Категорія | Критичних | Середніх | Низьких | Всього |
|-----------|-----------|----------|---------|--------|
| Перевикористання | 1 | 1 | 0 | 2 |
| Організація стилів | 0 | 1 | 2 | 3 |
| Фрагментованість | 0 | 1 | 2 | 3 |
| Модульність | 0 | 1 | 1 | 2 |
| **РАЗОМ** | **1** | **4** | **5** | **10** |

---

## 🔴 КРИТИЧНІ ПОРУШЕННЯ (Виправити негайно)

### 1.1 Дублювання функції `debounce`

**Категорія:** Перевикористання коду
**Пріоритет:** 🔴 КРИТИЧНИЙ
**Статус:** ❌ Не виправлено

**Опис проблеми:**
Утилітарна функція `debounce` визначена **4 рази** в різних модулях замість одного разу в спільних утилітах.

**Знайдено у файлах:**

1. **`js/generators/generator-table/gt-utils.js:52`**
```javascript
export function debounce(func, delay) {
    let timeoutId;
    return (...args) => {
        clearTimeout(timeoutId);
        timeoutId = setTimeout(() => func.apply(this, args), delay);
    };
}
```

2. **`js/generators/generator-seo/gse-events.js:10`**
```javascript
function debounce(func, wait) {
    let timeout;
    return function executedFunction(...args) {
        const later = () => {
            clearTimeout(timeout);
            func(...args);
        };
        clearTimeout(timeout);
        timeout = setTimeout(later, wait);
    };
}
```

3. **`js/generators/generator-text/gte-validator.js:166`**
```javascript
function debounce(func, delay) {
    let timeoutId;
    return (...args) => {
        clearTimeout(timeoutId);
        timeoutId = setTimeout(() => func(...args), delay);
    };
}
```

4. **`js/generators/generator-image/gim-logic.js:639`**
```javascript
function debounce(func, wait) {
    let timeout;
    return function(...args) {
        clearTimeout(timeout);
        timeout = setTimeout(() => func.apply(this, args), wait);
    };
}
```

**Як це порушує принципи:**
- ❌ Порушує DRY (Don't Repeat Yourself)
- ❌ Порушує принцип "Перевикористання: не дублювати код"
- ❌ Ускладнює підтримку (зміни треба робити в 4 місцях)
- ❌ Збільшує розмір фінального бандлу
- ❌ При виправленні бага треба змінювати 4 файли

**План виправлення:**

1. **Створити `js/utils/common-utils.js`:**
```javascript
/**
 * Загальні утиліти для всіх модулів
 * @module common-utils
 */

/**
 * Затримує виконання функції до закінчення періоду неактивності
 * @param {Function} func - Функція для виконання
 * @param {number} delay - Затримка в мілісекундах
 * @returns {Function} Debounced функція
 */
export function debounce(func, delay) {
    let timeoutId;
    return (...args) => {
        clearTimeout(timeoutId);
        timeoutId = setTimeout(() => func(...args), delay);
    };
}
```

2. **Оновити `gt-utils.js`:**
```javascript
// ВИДАЛИТИ визначення debounce
// ДОДАТИ:
export { debounce } from '../../utils/common-utils.js';
```

3. **Оновити `gse-events.js`:**
```javascript
// ВИДАЛИТИ визначення debounce (рядок 10-21)
// ДОДАТИ на початку файлу:
import { debounce } from '../../utils/common-utils.js';
```

4. **Оновити `gte-validator.js`:**
```javascript
// ВИДАЛИТИ визначення debounce (рядок 166-172)
// ДОДАТИ на початку файлу:
import { debounce } from '../../utils/common-utils.js';
```

5. **Оновити `gim-logic.js`:**
```javascript
// ВИДАЛИТИ визначення debounce (рядок 639-645)
// ДОДАТИ на початку файлу:
import { debounce } from '../../utils/common-utils.js';
```

**Очікуваний результат:**
- ✅ Один source of truth для `debounce`
- ✅ Легше підтримувати та оновлювати
- ✅ Менший розмір бандлу

---

## 🟠 СЕРЕДНІ ПОРУШЕННЯ (Виправити найближчим часом)

### 2.1 Дублювання базових стилів кнопок

**Категорія:** Перевикористання коду (CSS)
**Пріоритет:** 🟠 СЕРЕДНІЙ
**Статус:** ❌ Не виправлено

**Опис проблеми:**
Базові стилі кнопок (flex, padding, transition, тощо) дублюються в КОЖНОМУ файлі кнопок.

**Знайдено у файлах:**
- `css/components/buttons/button-primary.css`
- `css/components/buttons/button-secondary.css`
- `css/components/buttons/button-outline.css`

**Дубльований код:**
```css
/* Повторюється в КОЖНОМУ файлі */
display: inline-flex;
align-items: center;
justify-content: center;
gap: 8px;
padding: 8px 16px;
border-radius: 20px;
font-family: 'DM Sans', sans-serif;
font-weight: 500;
font-size: 14px;
cursor: pointer;
transition: all 0.2s ease;

.material-symbols-outlined {
    font-size: 18px;
}

:active {
    transform: scale(0.98);
}

:disabled {
    opacity: 0.38;
    cursor: not-allowed;
}
```

**Як це порушує принципи:**
- ❌ Порушує DRY в CSS
- ❌ Ускладнює зміни (треба міняти в 3 місцях)
- ❌ Збільшує розмір CSS

**План виправлення:**

1. **Створити `css/components/buttons/button-base.css`:**
```css
/**
 * COMPONENT: Button Base
 * Базові стилі для всіх кнопок
 */

.btn-base {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    padding: 8px 16px;
    border-radius: 20px;
    font-family: 'DM Sans', sans-serif;
    font-weight: 500;
    font-size: 14px;
    cursor: pointer;
    transition: all 0.2s ease;
    border: none;
}

.btn-base .material-symbols-outlined {
    font-size: 18px;
}

.btn-base:active {
    transform: scale(0.98);
}

.btn-base:disabled {
    opacity: 0.38;
    cursor: not-allowed;
}
```

2. **Оновити `button-primary.css`:**
```css
/* ВИДАЛИТИ всі базові стилі */
/* ЗАЛИШИТИ тільки специфічні для primary */
.btn-primary {
    background: var(--color-primary);
    color: var(--color-on-primary);
}

.btn-primary:hover:not(:disabled) {
    background: var(--color-primary-hover);
}
```

3. **Оновити всі HTML:**
```html
<!-- БУЛО -->
<button class="btn-primary">

<!-- СТАЛО -->
<button class="btn-base btn-primary">
```

4. **Додати в `css/main.css`:**
```css
@import url('components/buttons/button-base.css');  /* Додати ПЕРЕД іншими кнопками */
@import url('components/buttons/button-primary.css');
```

**Очікуваний результат:**
- ✅ Базові стилі визначені один раз
- ✅ Легше змінювати (одне місце)
- ✅ Менший розмір CSS

---

### 2.2 Інлайн стилі в `index.html`

**Категорія:** Організація стилів
**Пріоритет:** 🟠 СЕРЕДНІЙ
**Статус:** ❌ Не виправлено

**Опис проблеми:**
В `index.html` знайдено **20+ inline стилів** для статичних властивостей.

**Знайдені входження:**

| Рядок | Код | Проблема |
|-------|-----|----------|
| 173 | `style="color: var(--text-disabled);"` | Повторюється 6 разів |
| 195 | `style="color: var(--text-disabled);"` | Повторюється 6 разів |
| 204 | `style="display: flex;justify-content: space-between;..."` | Складна комбінація |
| 206 | `style="display: flex; gap: 4px; flex-wrap: nowrap;"` | Має бути клас |
| 323 | `style="border: none; width: 100%; height: 100%; border-radius: 12px;"` | Iframe стилі |
| 372 | `style="padding: 8px;"` | Простий padding |
| 376 | `style="background: transparent;padding: 0;..."` | Комбінація |
| 393 | `style="box-shadow: none;"` | Canvas стилі |

**Як це порушує принципи:**
- ❌ Порушує "Стилі в окремих файлах"
- ❌ Неможливо змінити глобально
- ❌ Ускладнює підтримку
- ❌ Не можна переопределити без `!important`

**План виправлення:**

**1. Створити utility класи (`css/utilities/helpers.css`):**
```css
.text-disabled {
    color: var(--text-disabled);
}

.u-p-8 {
    padding: 8px;
}
```

**2. Створити компонентні класи:**

`css/components/navigation/toolbar.css`:
```css
.format-toolbar {
    display: flex;
    gap: 4px;
    flex-wrap: nowrap;
}

.toolbar-wrapper {
    display: flex;
    justify-content: space-between;
    border-bottom: 1px solid var(--color-outline);
    padding: 8px;
    height: 48px;
    align-items: center;
}
```

`css/components/content/image-tool.css`:
```css
.image-tool-canvas {
    box-shadow: none;
}

.image-tool-iframe {
    border: none;
    width: 100%;
    height: 100%;
    border-radius: 12px;
}

.panel-box--transparent {
    background: transparent;
    padding: 0;
    padding-left: 8px;
}
```

**3. Оновити HTML:**
```html
<!-- БУЛО -->
<button style="color: var(--text-disabled);">

<!-- СТАЛО -->
<button class="text-disabled">
```

**Очікуваний результат:**
- ✅ Всі стилі в CSS файлах
- ✅ Можна легко змінювати глобально
- ✅ Чистий HTML

---

### 2.3 Жорстке зв'язування через DOM селектори

**Категорія:** Фрагментованість
**Пріоритет:** 🟠 СЕРЕДНІЙ
**Статус:** ❌ Не виправлено

**Опис проблеми:**
Модуль `gt-event-handler.js` **знає про внутрішню структуру** модальної системи, викликаючи `click()` на внутрішньому елементі.

**Знайдено у файлі:**
`js/generators/generator-table/gt-event-handler.js`

**Рядки 71, 76, 99:**
```javascript
document.querySelector('#global-modal-wrapper .modal-close-btn')?.click();
```

**Як це порушує принципи:**
- ❌ Tight coupling (жорстка залежність)
- ❌ Порушує інкапсуляцію модальної системи
- ❌ Неможливо змінити структуру modal без модифікації генератора
- ❌ Генератор знає про ID та класи модальної системи

**План виправлення:**

**1. Використовувати публічний API з `ui-modal.js`:**
```javascript
// БУЛО:
document.querySelector('#global-modal-wrapper .modal-close-btn')?.click();

// СТАЛО:
import { closeModal } from '../../common/ui-modal.js';
closeModal();
```

**2. Переконатись що `ui-modal.js` експортує `closeModal`:**
```javascript
// js/common/ui-modal.js
export function closeModal() {
    const activeModal = document.querySelector('.modal.is-visible');
    if (activeModal) {
        activeModal.classList.remove('is-visible');
        // ... решта логіки закриття
    }
}
```

**Очікуваний результат:**
- ✅ Loose coupling через API
- ✅ Можна змінювати modal без зміни генератора
- ✅ Чіткий контракт між модулями

---

### 2.4 Відсутність чіткої відповідальності в `ui-modal.js`

**Категорія:** Модульність
**Пріоритет:** 🟠 СЕРЕДНІЙ
**Статус:** ❌ Не виправлено

**Опис проблеми:**
Файл `ui-modal.js` містить **3 різні відповідальності** замість однієї.

**Файл:** `js/common/ui-modal.js`

**Функції:**
1. **Базове управління модалами** (54-128 рядки, 74 рядки):
   - `showModal(modalId)`
   - Завантаження шаблонів
   - Заповнення контенту

2. **Підтвердження дій** (161-258 рядки, 97 рядків):
   - `showConfirmModal(title, message, onConfirm)`
   - Динамічне створення HTML

3. **Глобальна ініціалізація** (260-287 рядки, 27 рядків):
   - `initModals()`
   - Реєстрація слухачів подій

**Як це порушує принципи:**
- ❌ Порушує Single Responsibility Principle
- ❌ Файл має більше однієї причини для змін
- ❌ Важко тестувати окремо кожну частину
- ❌ Великий файл (287 рядків) з різною логікою

**План виправлення:**

**Розділити на 3 файли:**

1. **`js/common/ui-modal.js`** (базовий функціонал):
```javascript
export function showModal(modalId) { ... }
export function closeModal() { ... }
```

2. **`js/common/ui-modal-confirm.js`** (діалоги підтвердження):
```javascript
import { showModal } from './ui-modal.js';

export function showConfirmModal(title, message, onConfirm) { ... }
```

3. **`js/common/ui-modal-init.js`** (ініціалізація):
```javascript
import { closeModal } from './ui-modal.js';

export function initModals() { ... }
```

**Очікуваний результат:**
- ✅ Кожен файл має одну відповідальність
- ✅ Легше тестувати
- ✅ Менші файли, простіше розуміти

---

## 🟡 НИЗЬКІ ПОРУШЕННЯ (Можна виправити пізніше)

### 3.1 Динамічні інлайн стилі в JS для анімацій

**Категорія:** Організація стилів
**Пріоритет:** 🟡 НИЗЬКИЙ
**Статус:** ❌ Не виправлено

**Опис проблеми:**
Анімація обертання кнопки реалізована через прямі маніпуляції зі стилями.

**Файл:** `js/generators/generator-table/gt-row-manager.js`

**Рядки 96-97, 110-112:**
```javascript
dom.reloadBtn.style.color = 'var(--primary-color)';
icon.style.transition = 'transform 0.5s ease';
icon.style.transform = 'rotate(360deg)';
```

**План виправлення:**
Створити CSS анімацію та керувати через класи.

---

### 3.2 Інлайн стилі в динамічному HTML (`ui-modal.js`)

**Категорія:** Організація стилів
**Пріоритет:** 🟡 НИЗЬКИЙ
**Статус:** ❌ Не виправлено

**Файл:** `js/common/ui-modal.js`

**Рядки 126, 183:**
```javascript
contentTarget.innerHTML = `<p style="color: var(--color-error);">Помилка...</p>`;

bodyTarget.innerHTML = `
    <div style="padding: 24px; text-align: center;">
        <p style="font-size: 16px; margin: 0 0 24px 0;">${message}</p>
        <div style="display: flex; gap: 12px; justify-content: center;">
`;
```

**План виправлення:**
Створити CSS класи для модальних повідомлень.

---

### 3.3 Залежність від глобального `Sortable`

**Категорія:** Фрагментованість
**Пріоритет:** 🟡 НИЗЬКИЙ
**Статус:** ❌ Не виправлено

**Файл:** `js/generators/generator-table/gt-main.js`

**Рядки 34-39:**
```javascript
if (typeof Sortable !== 'undefined') {
    new Sortable(dom.rowsContainer, {...});
}
```

**Проблема:**
Покладається на глобальну змінну `Sortable` з CDN.

**План виправлення:**
Динамічний імпорт або dependency injection.

---

### 3.4 Залежність від глобального `PapaParse`

**Категорія:** Фрагментованість
**Пріоритет:** 🟡 НИЗЬКИЙ
**Статус:** ❌ Не виправлено

**Файл:** `index.html`

**Рядок 426:**
```html
<script src="https://cdn.jsdelivr.net/npm/papaparse@5.4.1/papaparse.min.js"></script>
```

**Проблема:**
Глобальна залежність без явного імпорту.

---

### 3.5 Змішування рівнів абстракції в `gt-event-handler.js`

**Категорія:** Модульність
**Пріоритет:** 🟡 НИЗЬКИЙ
**Статус:** ❌ Не виправлено

**Файл:** `js/generators/generator-table/gt-event-handler.js`

**Проблема:**
Містить високорівневі обробники, низькорівневу логіку UI та бізнес-логіку в одному файлі.

**План виправлення:**
Розділити на:
- `gt-event-handler.js` - тільки реєстрація слухачів
- `gt-template-helpers.js` - addSampleList, addSampleTemplate
- `gt-preview-handler.js` - handleTablePreview

---

## ✅ ХОРОШІ ПРАКТИКИ В ПРОЄКТІ

### 1. Відмінна CSS організація
- ✅ Foundation → Layout → Components → Utilities
- ✅ Кожен компонент в окремому файлі
- ✅ Детальна документація

### 2. Модульна архітектура генераторів
- ✅ Розділення на `*-main.js`, `*-dom.js`, `*-state.js`, тощо
- ✅ Чітка структура папок

### 3. Система реєстрації панелей
- ✅ `registerPanelInitializer()` дозволяє loose coupling
- ✅ Генератори саморегеструються

### 4. Кешування DOM
- ✅ Всі `*-dom.js` файли кешують елементи

---

## 📋 ПРІОРИТЕТНИЙ ПЛАН ВИПРАВЛЕНЬ

### ФАЗА 1: Критичні (✅ Виправляється зараз)
- [x] Винести `debounce` в `js/utils/common-utils.js`
- [ ] Оновити всі 4 файли для використання спільної функції

### ФАЗА 2: Середні (Наступні 1-2 тижні)
- [ ] Створити `button-base.css`
- [ ] Винести інлайн стилі з `index.html`
- [ ] Замінити прямі виклики modal DOM на API
- [ ] Розділити `ui-modal.js` на 3 файли

### ФАЗА 3: Низькі (Коли буде час)
- [ ] Анімації через CSS класи
- [ ] Зробити явні залежності від Sortable/PapaParse
- [ ] Розділити `gt-event-handler.js`

---

## 📊 МЕТРИКИ ЯКОСТІ

### До виправлень:
- Дублювання коду: **4 входження** debounce
- Inline стилів: **~30** в HTML, **~10** в JS
- Жорстких залежностей: **3** (modal DOM, Sortable, PapaParse)
- Файлів з множинною відповідальністю: **2**

### Після виправлень (очікувано):
- Дублювання коду: **0** ✅
- Inline стилів: **~5** (тільки динамічні) ✅
- Жорстких залежностей: **0** ✅
- Файлів з множинною відповідальністю: **0** ✅

---

## 🎓 ВИСНОВОК

**Загальна оцінка архітектури:** 8.5/10

**Сильні сторони:**
- Продумана модульна структура
- Відмінна організація CSS
- Система реєстрації панелей
- Централізоване кешування DOM

**Що покращити:**
- Усунути дублювання утиліт (КРИТИЧНО)
- Винести inline стилі в CSS
- Розірвати жорсткі зв'язки
- Розділити файли з множинною відповідальністю

Проєкт має **відмінну базу** і після виправлення критичних та середніх порушень стане еталонним прикладом архітектури.

---

**Дата створення:** 2025-01-15
**Наступний перегляд:** 2025-02-01
**Відповідальний:** Architecture Team
