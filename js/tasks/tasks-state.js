// js/tasks/tasks-state.js

/**
 * ╔══════════════════════════════════════════════════════════════════════════╗
 * ║                    TASKS - STATE                                         ║
 * ╚══════════════════════════════════════════════════════════════════════════╝
 *
 * Глобальний стан для модуля задач.
 *
 * 🔒 ЯДРО — цей файл не можна видаляти!
 *
 * СТРУКТУРА КОЛОНОК Tasks (Google Sheets):
 * A: id | B: title | C: description | D: type | E: status | F: priority
 * G: created_by | H: assigned_to | I: created_at | J: updated_at
 * K: due_date | L: tags | M: code_snippet
 */

/**
 * Глобальний стан для tasks модуля
 */
export const tasksState = {
    // ═══════════════════════════════════════════════════════════════════════
    // АКТИВНИЙ ТАБ
    // ═══════════════════════════════════════════════════════════════════════

    activeTab: 'my', // 'my' | 'inbox' | 'sent' | 'info'

    // ═══════════════════════════════════════════════════════════════════════
    // ДАНІ
    // ═══════════════════════════════════════════════════════════════════════

    tasks: [],
    users: [], // Для вибору assigned_to

    // ═══════════════════════════════════════════════════════════════════════
    // ПОТОЧНИЙ КОРИСТУВАЧ
    // ═══════════════════════════════════════════════════════════════════════

    currentUserId: null,

    // ═══════════════════════════════════════════════════════════════════════
    // КАБІНЕТ — СТАТИСТИКА ПРАЙСУ
    // Заповнюється через tasks-cabinet.js плагін (loadPriceStats)
    // ═══════════════════════════════════════════════════════════════════════

    priceStats: {
        totalReserved: 0,   // Зарезервовані товари поточного юзера
        totalPosted: 0,     // Опубліковані (status=TRUE)
        totalChecked: 0,    // Перевірені (check=TRUE)
        noArticle: 0,       // Без артикулу (article порожній)
        canPost: 0,         // Можна викласти (є артикул, але status != TRUE)
        loaded: false       // Чи завантажено дані
    },

    // ═══════════════════════════════════════════════════════════════════════
    // ПОШУК
    // ═══════════════════════════════════════════════════════════════════════

    searchQuery: '',
    searchColumns: ['id', 'title', 'description', 'tags'],

    // ═══════════════════════════════════════════════════════════════════════
    // ФІЛЬТРИ
    // ═══════════════════════════════════════════════════════════════════════

    filters: {
        status: [], // ['todo', 'in_progress', 'done']
        priority: [], // ['low', 'medium', 'high', 'urgent']
        type: [] // ['task', 'info', 'script', 'reference']
    },

    // ═══════════════════════════════════════════════════════════════════════
    // ПАГІНАЦІЯ
    // ═══════════════════════════════════════════════════════════════════════

    pagination: {
        currentPage: 1,
        pageSize: 25,
        totalItems: 0
    },

    // API пагінації
    paginationAPI: null,
};
